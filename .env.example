# Operator's signing key. Only required for `settle` in live mode.
# Dry-run and emit-calldata modes don't need a key — operator signs offline
# via Safe Transaction Builder or hardware wallet from the emitted bundle.
PRIVATE_KEY=

# Path to runner config JSON. If unset, --config is required at the CLI.
RUNNER_CONFIG=./config.example.yaml
